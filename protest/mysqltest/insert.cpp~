#include <mysql/mysql.h>
#include <stdio.h>
#include <stdlib.h>

int main()
{
	MYSQL *c;


	c=mysql_init(NULL);

	mysql_real_connect(c,"localhost","root","root","test",0,NULL,0);

	mysql_query(c,"insert into grade (number,name,english,chinese,math) values (2,'yifeng',50,90,70)");

	return 0;
}
